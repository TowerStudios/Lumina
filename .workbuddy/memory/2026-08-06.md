# 2026-08-06 工作日志

## 对话页面对标分析（tdesktop-dev + WeFlow-5.1.0）
- 完成三方代码调研：TG `history/` 目录功能清单、Lumina ChatView 现状矩阵、WeFlow 解码链。
- 产出差距分析：
  - A 组（无跨页依赖，10 项）：右键菜单、多选、引用跳转（需 getMessagesAround IPC）、消息内搜索条、跳转日期、未读分隔线、媒体铺满气泡、链接/位置可点击、顶栏更多菜单、撤回联调。
  - B 组（WeFlow 解码参照，4 项）：silk 真实语音播放（decodeSilkToPcm→WAV）、视频内联播放、表情本地回退链、转发记录展开优化（forwardRecordParser 递归解析）。
  - C 组（跨页依赖，7 项，待用户拍板）：C1 联系人详情落地、C2 媒体查看器形态（Overlay/独立窗口/维持）、C3 转发展开形态、C4 语音转写对话框、C5 SNS 朋友圈入口、C6 消息写操作（建议不做，保持只读）、C7 设置项扩展。
- 等待用户对 C 组拍板后按 A1→A10、B1→B4 顺序开工。

## 用户对 C 组的拍板（2026-08-06 下午）
- C1-a：联系人详情复用 ChatDetailPanel（加联系人模式）
- C2-b：媒体查看器用独立 BrowserWindow（含视频应用内播放）
- C3-a：转发消息独立窗口（用户原话需求：一边看多级折叠转发，一边看群友吐槽 → 并排对照）
- C4 做（语音转写独立对话框）；C5 排 P4；C6 不做（保持只读查看器定位）；C7 做（展示偏好设置项）
- 已重写 TODO.md：新增「待验证/已知风险」区（8 项，含全局搜索定位静默失败的真实缺陷）+ S1–S6 阶段计划（22 个独立小任务，覆盖 A/B/C 组）。
- 关键发现：chatService.getMessagesAround 后端已实现（2758 行），只差 ipcHandlers/preload/electron.d.ts 接线（S2-T5）。
- 任务清单 #1–#19 已建立。下一步：从 S1 开始实施。
